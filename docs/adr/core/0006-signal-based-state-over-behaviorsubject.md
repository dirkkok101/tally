# ADR-CORE-0006: Signal-Based State Over BehaviorSubject

> Generated from the lex graph. Do not edit this file as source of truth; update graph entities and re-export.

**Status:** Accepted
**Origin:** Identity convention (ADR-0006), adopted by Actions as Angular 21 best practice.

## Context

Angular 21 provides stable signals (`signal()`, `computed()`, `linkedSignal()`, `effect()`) as first-class reactive primitives. Signals integrate directly with change detection, eliminating subscription management, `async` pipes, and manual `markForCheck()` calls.

BehaviorSubject was the standard before signals existed but requires explicit subscription lifecycle management (unsubscribe on destroy) and does not integrate with Angular's change detection natively.

## Decision

**All component state uses Angular signals.** BehaviorSubject is not used for local component state.

- `signal()` for mutable state
- `computed()` for derived state
- `linkedSignal()` for dependent state that resets on source change
- `effect()` sparingly — prefer computed signals for derivations
- RxJS remains for HTTP calls wrapped in `firstValueFrom()`

## Consequences

**Positive:**
- No subscription management — signals are synchronous and garbage-collected
- Direct change detection integration — OnPush "just works" with signals
- Cleaner templates — `mySignal()` instead of `myObservable | async`
- Fewer memory leaks — no forgotten unsubscriptions

**Negative:**
- Two reactive paradigms coexist (signals for state, RxJS for HTTP)
- `effect()` can create debugging surprises if overused (dependency tracking is implicit)

**Guidelines:**
- Use `effect()` only for side effects that cannot be expressed as computed signals (e.g., logging, external API calls)
- Never use `effect()` to synchronise two signals — use `computed()` or `linkedSignal()` instead
- HTTP services return `Promise<T>` via `firstValueFrom()` — callers assign to signals

## Pattern References

- [PAT-CORE-WEB-CAPTURE-PAGE: Capture Page Pattern — NXGN Actions Web](../../patterns/core/web-capture-page.md) (implements)
- [PAT-CORE-WEB-CAPTURE-STATE: Capture State Service Pattern — NXGN Actions Web](../../patterns/core/web-capture-state.md) (implements)
- [PAT-CORE-WEB-EMBEDDED-LIST: Embedded List Pattern — NXGN Actions Web](../../patterns/core/web-embedded-list.md) (implements)
- [PAT-CORE-WEB-FEATURE-IMPLEMENTATION: Feature Implementation Guide — NXGN Actions Web](../../patterns/core/web-feature-implementation.md) (implements)
- [PAT-CORE-WEB-FEATURE-SERVICE: Feature Service Pattern — NXGN Actions Web](../../patterns/core/web-feature-service.md) (implements)
- [PAT-CORE-WEB-LIST-PAGE: List Page Pattern — NXGN Actions Web](../../patterns/core/web-list-page.md) (implements)
- [PAT-CORE-WEB-ROUTING: Routing Pattern — NXGN Actions Web](../../patterns/core/web-routing.md) (implements)
- [PAT-CORE-WEB-UI-COMPONENTS: UI Component Reference — NXGN Actions Web](../../patterns/core/web-ui-components.md) (implements)
- [PAT-CORE-WEB-VALIDATION: Validation, Confirmation, and Modal Patterns — NXGN Actions Web](../../patterns/core/web-validation.md) (implements)

## Related

- [ADR-CORE-0012: Zoneless Angular](0012-zoneless-angular.md) (references)
