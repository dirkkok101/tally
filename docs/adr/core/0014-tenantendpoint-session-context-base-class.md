# ADR-CORE-0014: TenantEndpoint Session Context Base Class

> Generated from the lex graph. Do not edit this file as source of truth; update graph entities and re-export.

**Status:** Accepted
**Origin:** Adapted from Identity's `IdentityEndpoint` (Identity ADR-0014), extended with `DefaultLanguageId` for Actions' translation requirements.

## Context

Every management endpoint in Actions needs tenant context (who is making the request, for which tenant, in which language). Extracting JWT claims in every endpoint is error-prone and duplicative.

Identity solved this with an `IdentityEndpoint` base class that extracts `TenantId`, `ActorId`, and `LanguageId`. Actions needs the same pattern but with an additional claim — `DefaultLanguageId` — for the translation fallback chain (see ADR-0013).

## Decision

**Four `TenantEndpoint` base class variants** extract session context from JWT claims before the handler executes:

| Base Class | Use Case |
|-----------|----------|
| `TenantEndpoint<TRequest, TResponse>` | Save, GetGridList, Action endpoints |
| `TenantEndpoint<TRequest>` | Delete, Lifecycle endpoints |
| `TenantEndpointWithoutRequest<TResponse>` | Get, Lookup endpoints |
| `TenantEndpointWithoutRequest` | Lifecycle with no body |

**Extracted claims:**
- `TenantId` (Guid) — from `tenant_id` claim. Required.
- `UserId` (Guid) — from `sub` claim. Required.
- `RequestLanguageId` (Guid) — from `language_id` claim. Used for translation resolution.
- `DefaultLanguageId` (Guid) — from `default_language_id` claim. Fallback for translation chain.

**Behaviour:** Claims are extracted and validated before `HandleAsync` executes. Missing or invalid required claims return 401.

## Consequences

**Positive:**
- Session context available as typed properties in every endpoint — no manual claim parsing
- Language claims feed directly into mapper calls (DTOMapper and EntityMapper)
- Consistent 401 behaviour for missing claims across all endpoints
- Four variants cover all FastEndpoints signature combinations

**Negative:**
- Base class accumulates concerns (4 properties now). Resist adding more — timezone, theme, etc. belong in per-request services, not the base class.

## Related

- [ADR-CORE-0002: Actions RLS Boundary](0002-actions-rls-boundary.md) (references)
- [ADR-CORE-0013: Capstone Translation Pattern for Entity i18n](0013-capstone-translation-pattern-for-entity-i18n.md) (references)
