# ADR-CORE-0021: Explicit DI Registration — No Reflection Scanning

> Generated from the lex graph. Do not edit this file as source of truth; update graph entities and re-export.

**Status:** Accepted
**Origin:** Capstone convention, adopted by Identity (ADR-0024), adopted by Actions on independent merit.

## Context

DI registration can be explicit (`services.AddScoped<T>()`) or convention-based (assembly scanning, e.g., Scrutor's `services.Scan(...)`). Convention-based scanning reduces boilerplate but creates debugging difficulties when registration silently fails or picks up unintended types.

## Decision

**Explicit registration via per-feature extension methods.** No reflection scanning.

**Structure:**
- `Bootstrap/Services.cs` — central orchestrator, chains all feature registrations
- `Bootstrap/Features/{Feature}Extensions.cs` — one static extension method per feature
- Called from `Program.cs` via `builder.Services.AddServices()`

**Per-feature method registers:**
- Commands (Save, Delete, Lifecycle)
- Queries (Get, GridList, Count, Lookup)
- Mappers (EntityMapper, DTOMapper, DTOMapperDataContextService)

**Rules:**
- Always `AddScoped` (matches DbContext lifetime, forces stateless design)
- Endpoints and Validators are auto-discovered by FastEndpoints (framework-mandated, no opt-out, clear naming conventions — distinct from optional DI scanning)
- `IEntityTypeConfiguration<T>` classes are auto-discovered by EF Core's `ApplyConfigurationsFromAssembly()` (see ADR-0018 — framework-supported, implements a known interface with clear semantics)

## Consequences

**Positive:**
- Auditable — search for `AddScoped<MyService>` to find (or confirm absence of) registration
- Compile-time safe — missing registration produces a clear DI resolution error on first request
- Debuggable — no magic conventions to understand
- Feature isolation — each feature's registrations are self-contained

**Negative:**
- Boilerplate — every new command/query/mapper needs a registration line
- Forgetting registration is caught at runtime, not compile time

**Trade-off accepted:** The boilerplate is mechanical and discoverable. Forgetting registration fails immediately during development (first request to the endpoint). Integration tests that exercise every endpoint catch missing registrations before deployment. Convention scanning failures are silent and insidious.

## Related

- [ADR-CORE-0009: Org-Scoped Platform Admin Tenancy](0009-org-scoped-platform-admin-tenancy.md) (references)
- [ADR-CORE-0018: IEntityTypeConfiguration for EF Core](0018-ientitytypeconfiguration-for-ef-core.md) (references)
- [ADR-CORE-0024: Vertical Slice Architecture](0024-vertical-slice-architecture.md) (references)
- [ADR-CORE-0027: Use DbContext Factory and Optional Shared Context for Queries and Commands](0027-use-dbcontext-factory-and-optional-shared-context-for-queries-and-commands.md) (references)
- [C5: Selective Actions prior-art reuse](../../prd/insights/prd.md) (see-also)
- [DD-BUDGET-APPLICATION-ARCHITECTURE: Typed vertical slices with one earned public-contract seam](../../designs/budget/decisions/application-architecture.md) (references)
- [DD-CLASSIFY-APPLICATION-ARCHITECTURE: Single-process vertical slices with one earned external seam](../../designs/classify/decisions/application-architecture.md) (driven_by)
- [DD-INGEST-APPLICATION-ARCHITECTURE: Single-process vertical slices with isolated ingestion state](../../designs/ingest/decisions/application-architecture.md) (driven_by)
- [DD-INSIGHTS-APPLICATION-ARCHITECTURE: Typed analytical compiler with concrete local boundaries](../../designs/insights/decisions/application-architecture.md) (references)
- [DD-LEDGER-APPLICATION-ARCHITECTURE: Single-process provider-neutral vertical slices with selective ports](../../designs/ledger/decisions/application-architecture.md) (references)
- [MD-CLASSIFY-MASTER: Transaction Classification technical design](../../designs/classify/design.md) (driven_by)
- [MD-INGEST-MASTER: Statement Ingestion technical design](../../designs/ingest/design.md) (see-also)
- [MD-INSIGHTS-MASTER: Budget Insights technical design](../../designs/insights/design.md) (see-also)
- [MD-LEDGER-MASTER: Provider-neutral financial ledger technical design](../../designs/ledger/design.md) (see-also)
- [PLAN-LEDGER-V1: Provider-neutral Financial Ledger v1 implementation](../../plans/ledger/overview.md) (see-also)
