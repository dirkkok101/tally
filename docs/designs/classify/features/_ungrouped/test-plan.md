# Ungrouped — Test Plan

> Generated from the lex graph. Do not edit this file as source of truth; update graph entities and re-export.

## Summary

| Metric | Count |
|---|---:|
| Total test cases | 8 |
| E2E | 7 |
| INTEGRATION | 1 |

## E2E

### Edge

#### Verify additive CLASSIFY ergonomics compatibility and inventory

**Ref Code:** TC-CLASSIFY-ERGONOMICS-PUBLISHED-CONTRACT
**Surface:** contract
**Automation:** automated
**Status:** planned
**Owner:** CLASSIFY
**Implementation Ref:** tests/Tally.Tests/Classify/Contract/ClassifyErgonomicsPublishedContractTests.cs

##### Preconditions

The implementation contains the released twelve-operation baseline plus the five accepted ergonomics operations.

##### Steps

Run schema list/show, strict source-generated JSON round trips, unknown-field failures, handler composition, declared-error-to-exit parity, Linux Native-AOT invocation, old twelve-operation request fixtures, migration/open of existing classify.db, privacy scans, module gates, and rollback compatibility checks.

##### Expected

All twelve 0.3.3 identifiers retain their 1.0 request/result/error behavior; exactly five additive operations are discoverable and composed; old stores open without data migration where specified; unsupported versions fail before data access; rollback remains possible because no new durable query/report schema is required and corpus outputs stay external private files.

##### Cleanup

Remove temporary publish and isolated roots; retain no private corpus.

#### Verify paginated outcome discovery

**Ref Code:** TC-CLASSIFY-OUTCOME-LIST-CONTRACT
**Surface:** contract
**Automation:** automated
**Status:** planned
**Owner:** CLASSIFY
**Implementation Ref:** tests/Tally.Tests/Classify/Evaluation/OutcomeListTests.cs

##### Preconditions

A completed evaluation has suggestion, no_suggestion, conflict, and stale-capable rows plus active category and rule evidence in an isolated data root.

##### Steps

Invoke the published schema and Native-AOT classify.outcome.list boundary for empty, one-page, and multi-page evaluations; apply every supported filter; walk every continuation; pass returned outcome IDs unchanged to selected_outcomes preview.

##### Expected

Pages are ordered by ordinal then transaction ID; totals and fields are exact; every row appears once; filters compose with AND semantics; list-to-preview composition needs no per-outcome process and performs zero Ledger mutation.

##### Cleanup

Remove only the isolated data root.

#### Verify rule catalogue and active rule-set discovery

**Ref Code:** TC-CLASSIFY-RULEBOOK-DISCOVERY-CONTRACT
**Surface:** contract
**Automation:** automated
**Status:** planned
**Owner:** CLASSIFY
**Implementation Ref:** tests/Tally.Tests/Classify/Rules/RuleDiscoveryTests.cs

##### Preconditions

An isolated store contains multiple logical rules, immutable versions, lifecycle events, one active set, trusted validation and gate receipt metadata, active and archived categories, and a concurrent append fixture.

##### Steps

Invoke rule.list with every filter and cursor boundary; append a later rule after page one; invoke rule-set.active.get for active and empty stores; inspect source-generated schemas and public errors.

##### Expected

The high-water cursor yields deterministic non-duplicated pages; lifecycle and active membership derive from append-only authority; the active summary returns ruleSetVersionId, broadApplyAllowed, activationId, validationId, trusted gate receipt ID and fingerprint, normalization version, ordered rule-version IDs, activation epoch, lifecycle timestamps/status, and category ID/display name/lifecycle enum; empty authority is typed not-found; no private validation or description payload appears.

##### Cleanup

Remove only the isolated data root.

### Security

#### Verify bounded selected-outcome preview composition

**Ref Code:** TC-CLASSIFY-BULK-PREVIEW-COMPOSITION
**Surface:** contract
**Automation:** automated
**Status:** planned
**Owner:** CLASSIFY
**Implementation Ref:** tests/Tally.Tests/Classify/Apply/OutcomeListPreviewCompositionTests.cs

##### Preconditions

A completed evaluation contains at least 201 suggestions under an active rule set, with broadApplyAllowed both false and true in separate isolated fixtures.

##### Steps

List suggestions, preview exact batches of 1 and 200 IDs, submit duplicate, mixed-evaluation, unknown, stale, archived-category, non-suggestion, and 201-ID selections, then drift authority before apply.run.

##### Expected

The existing selected_outcomes schema is unchanged; valid previews freeze exact evidence without Ledger mutation; invalid or oversized batches have no partial preview; broadApplyAllowed=false never authorizes implicit or unattended apply; apply-time drift fails closed.

##### Cleanup

Remove only the isolated data root.

#### Verify private corpus builder binding and atomic recovery

**Ref Code:** TC-CLASSIFY-PRIVATE-CORPUS-BUILDER-CONTRACT
**Surface:** persistence
**Automation:** automated
**Status:** planned
**Owner:** CLASSIFY
**Implementation Ref:** tests/Tally.Tests/Classify/Validation/PrivateCorpusBuilderTests.cs

##### Preconditions

An isolated Linux owner-only data root provides a public evaluation-purpose classification_v1 projection and explicit labels; injectable file-write boundaries are enabled.

##### Steps

Build canonical subsets in different label orders; replay identical and conflicting idempotency keys; exercise empty, duplicate, malformed, missing-member, inactive-category, oversized, symlink, wrong-owner, permissive, existing-destination, cancellation, write, flush, rename, and post-rename interruption cases; read success with PrivateCorpusReader.

##### Expected

Only explicit labels become dense canonical rows; success bytes and fingerprints are deterministic and reader-compatible; the destination is owner-owned 0600; every failure has no partial final file or unrelated deletion; retry recovers a matching post-rename commit; output and diagnostics remain aggregate-only.

##### Cleanup

Remove the exact isolated root and any test-created owner-only temporary.

#### Verify deterministic private unresolved-pattern reporting

**Ref Code:** TC-CLASSIFY-UNRESOLVED-PATTERN-REPORT-CONTRACT
**Surface:** contract
**Automation:** automated
**Status:** planned
**Owner:** CLASSIFY
**Implementation Ref:** tests/Tally.Tests/Classify/Reporting/UnresolvedPatternReportTests.cs

##### Preconditions

An isolated completed evaluation contains retained no_suggestion outcomes whose fresh compatible projection exercises normalization equivalence, account and direction splits, count ties, amount sums, and below-minimum groups.

##### Steps

Invoke unresolved.report at exact and one-over bounds; replay unchanged semantics with a fresh snapshot ID; inject every retained/projection/rule-set/category/generation/normalization drift and join-accounting failure; inspect durable files, databases, logs, and rule history.

##### Expected

Groups and totals obey exact accounting and deterministic order; semantic replay hashes match; only the explicit success result contains normalized descriptions and aggregate amounts; all failures return no result; no raw report store, rule proposal, activation, preview, apply, or Ledger mutation exists.

##### Cleanup

Remove only the isolated data root.

### Performance

#### Verify installed-process operator throughput bounds

**Ref Code:** TC-CLASSIFY-OPERATOR-THROUGHPUT-PROCESS
**Surface:** service
**Automation:** automated
**Status:** planned
**Owner:** CLASSIFY
**Implementation Ref:** tests/Tally.Tests/Classify/Process/ClassifyOperatorThroughputTests.cs

##### Preconditions

A linux-x64 Native-AOT publish and isolated 146-row plus 10000-row evaluations are available with process-count and peak-memory measurement.

##### Steps

Use only the published CLI to retrieve the 146-row result with pageSize 500 and to walk maximum-size filtered results at page sizes 1 and 500; record process launches, continuations, wall time, memory, row accounting, and child processes.

##### Expected

The 146-row case uses one classify.outcome.list process; general walks use exactly ceiling(filteredCount/pageSize) processes; each call is within 5 seconds and 256 MiB; no per-row CLI or Ledger subprocess occurs; one-over limits fail with no partial result or mutation.

##### Cleanup

Remove the temporary publish and isolated data root.

## INTEGRATION

### Unhappy

#### Reject invalid or stale outcome continuations without partial results

**Ref Code:** TC-CLASSIFY-OUTCOME-CURSOR-STALE-MATRIX
**Surface:** contract
**Automation:** automated
**Status:** planned
**Owner:** CLASSIFY
**Implementation Ref:** tests/Tally.Tests/Classify/Evaluation/OutcomeListCursorTests.cs

##### Preconditions

A retained evaluation and at least one valid outcome-list continuation exist in an isolated data root.

##### Steps

Tamper each cursor field; replay under another evaluation, filter, page size, category lifecycle, active rule set, Ledger generation, expired snapshot, and incompatible projection; inject cancellation and malformed retained evidence.

##### Expected

Each case returns the specified typed cursor, stale, lifecycle, compatibility, resource, or integrity error, a null result, zero returned rows, and byte-identical CLASSIFY and Ledger mutation state.

##### Cleanup

Remove only the isolated data root.
