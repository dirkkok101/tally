# Action Creation (M2M) — Process Flow

> Generated from the lex graph. Do not edit this file as source of truth; update graph entities and re-export.

**Diagram Type:** workflow
**Format:** mermaid

## Content

```mermaid
flowchart TD
    start([START: M2M service sends<br/>POST /api/v1/actions<br/>with client_credentials JWT])
    s1[1. Validate JWT<br/>Verify Bearer token issued by NxGN Identity<br/>Extract tenant_id from claims]
    s2[2. Validate M2M scopes<br/>Required scope: actions:write]
    d1{Scopes valid?}
    stop403([END - 403 Forbidden])
    s3[3. Derive origin_system from client_id mapping<br/>e.g., capstone-api → origin_system=capstone<br/>e.g., pulse-api → origin_system=pulse]
    s4[4. Execute same creation flow as PROC-1 steps 4–10<br/>With additional field: origin_system = derived value]
    s5[5. Optionally create source link<br/>If request body includes source_link:<br/>INSERT actions.action_source_links<br/>action_id, origin_system, origin_entity_type,<br/>origin_entity_id, origin_url]
    s6[6. Return 201 Created with action DTO + ETag]
    stop([END])

    start --> s1 --> s2 --> d1
    d1 -->|No| stop403
    d1 -->|Yes| s3 --> s4 --> s5 --> s6 --> stop

```
