# Action Number Generation

> Generated from the lex graph. Do not edit this file as source of truth; update graph entities and re-export.

**Diagram Type:** workflow
**Format:** mermaid

## Content

```mermaid
flowchart TD
    start([START: Action creation requires unique action number])
    s1[1. Begin transaction]
    s2[2. SELECT FOR UPDATE on TenantSequence for current tenant<br/>SELECT current_value FROM actions.tenant_sequences<br/>WHERE tenant_id = tenant_id<br/>FOR UPDATE<br/>Row-level lock prevents concurrent reads until COMMIT]
    s3[3. Increment sequence counter<br/>current_value = current_value + 1<br/>UPDATE actions.tenant_sequences<br/>SET current_value = new_value<br/>WHERE tenant_id = tenant_id]
    s4[4. Format action number<br/>ACT-counter padded to 4+ digits<br/>e.g., ACT-0001, ACT-0042, ACT-1337, ACT-10000]
    s5[5. Commit transaction<br/>Releases row-level lock,<br/>allowing next concurrent request to proceed]
    s6[6. Return formatted action number]
    stop([END])

    start --> s1 --> s2 --> s3 --> s4 --> s5 --> s6 --> stop

```
