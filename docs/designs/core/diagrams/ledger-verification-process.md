# Ledger Verification (Background Service)

> Generated from the lex graph. Do not edit this file as source of truth; update graph entities and re-export.

**Diagram Type:** workflow
**Format:** mermaid

## Content

```mermaid
flowchart TD
    start([START: LedgerVerificationHostedService runs periodically])
    s1[1. Verify append-only constraints are active<br/>Check that UPDATE/DELETE triggers exist on:<br/>actions.action_state_history<br/>actions.action_audit_events<br/>Query pg_trigger system view]
    s2[2. Verify row integrity<br/>Check row counts match expectations<br/>Verify no gaps in sequence numbers per action]
    d1{Constraints verified?}

    subgraph fail ["Constraint failure path"]
        logerr[Log error with details<br/>LogError: Audit trail integrity constraint<br/>missing or inactive<br/>table, constraint_type]
        cont[Continue checking remaining constraints]
        logerr --> cont
    end

    s3[3. All constraints verified]
    s4[4. Log verification result<br/>LogDebug: Ledger verification passed<br/>tables_checked, rows_sampled]
    stop([END])

    start --> s1 --> s2 --> d1
    d1 -->|No| fail --> stop
    d1 -->|Yes| s3 --> s4 --> stop

```
