# System Data Flow

> Generated from the lex graph. Do not edit this file as source of truth; update graph entities and re-export.

**Diagram Type:** data_flow
**Format:** mermaid

## Content

```mermaid
flowchart TB
    subgraph Clients["Clients"]
        SPA["Angular SPA<br/>(Actions Web UI)"]
        M2M["M2M Services<br/>(Capstone / Pulse / other platforms)"]
    end

    subgraph Service["Actions Service"]
        BFF["actions-bff<br/>(YARP + session)"]
        API["actions-api<br/>(FastEndpoints + CQRS)"]
    end

    subgraph Stores["Infrastructure"]
        REDIS[("Redis<br/>BFF sessions")]
        PG[("PostgreSQL<br/>actions schema")]
        BLOB[("Azure Blob<br/>evidence files")]
    end

    IDSVC["Identity API"]

    SPA -- "cookie (HttpOnly)<br/>+ X-CSRF: 1" --> BFF
    M2M -- "X-API-Key<br/>or Bearer JWT" --> API
    BFF -- "OIDC flows" --> IDSVC
    BFF -- "session storage" --> REDIS
    BFF -- "Bearer JWT<br/>(from session)" --> API
    API -- "DB queries (RLS)" --> PG
    API -- "Blob SAS generation" --> BLOB
    API -- "API key validation" --> IDSVC

```
