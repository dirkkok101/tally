# Contract and Formats — Test Plan

> Generated from the lex graph. Do not edit this file as source of truth; update graph entities and re-export.

## Summary

| Metric | Count |
|---|---:|
| Total test cases | 5 |
| E2E | 3 |
| INTEGRATION | 2 |

## E2E

### Happy

#### Verify INGEST contract discovery

**Ref Code:** TC-INGEST-CONTRACT-DISCOVERY-CONTRACT
**Surface:** contract
**Automation:** automated
**Status:** passing
**Owner:** INGEST implementation

##### Preconditions

A published compatible Tally binary and incompatible-version fixtures are available.

##### Steps

Invoke help and schema discovery, then exercise valid, malformed, unknown-field, and incompatible-version requests through the published process contract.

##### Expected

Every acceptance criterion of FR-INGEST-CONTRACT-DISCOVERY holds, with one stdout envelope, metadata-only stderr, stable exits, and no source read or mutation.

#### Verify the published INGEST operation contract

**Ref Code:** TC-INGEST-PUBLISHED-CONTRACT-MATRIX
**Surface:** contract
**Automation:** automated
**Status:** passing

##### Steps

Invoke schema discovery and all eight operations through the published binary using private stdin/file JSON; exercise success and every stable failure category; inspect stdout, stderr, exits, offline behavior, cancellation, and process lifetime.

##### Expected

Descriptors, schemas, limits, errors, and exits remain versioned and consistent; each invocation emits one stdout envelope; stderr is metadata-only; unknown fields fail before processing; no prompt, daemon, network call, or orphan process appears.

### Performance

#### Verify PdfPig Native-AOT extraction conformance

**Ref Code:** TC-INGEST-PDF-EXTRACTION-AOT
**Surface:** contract
**Automation:** automated
**Status:** passing

##### Steps

Publish the linux-x64 self-contained Native-AOT Tally binary with PdfPig pinned; inject each qualified private fixture through the test-only fixture provider; preview every fixture twice while measuring wall time and peak RSS; compare page/glyph evidence digests; include the permission-encrypted fixture.

##### Expected

Every fixture yields non-empty deterministic evidence twice; the encrypted fixture needs no opening password; each run is under 5 seconds and 256 MiB; no trim/AOT failure, source argument, external process, or fixture disclosure occurs.

## INTEGRATION

### Edge

#### Verify both qualified adapters against golden fixtures

**Ref Code:** TC-INGEST-ADAPTER-GOLDEN-FIXTURES
**Surface:** service
**Automation:** automated
**Status:** planned

##### Steps

Run the real PdfPig extractor and both explicitly registered adapters over every injected private fixture plus changed-layout, ambiguous, zero-movement, duplicate-tuple, date-boundary, and control-mismatch mutations.

##### Expected

Exactly one adapter matches each qualified fixture; every record is accounted for; exact controls reconcile; identical tuples at different positions remain distinct; unsupported, ambiguous, changed, or unreconciled variants fail closed without LEDGER mutation.

#### Verify supported variant qualification

**Ref Code:** TC-INGEST-VARIANT-QUALIFICATION-CONTRACT
**Surface:** contract
**Automation:** automated
**Status:** passing
**Owner:** INGEST implementation

##### Preconditions

The three private fixtures are available outside tracked test artifacts together with redacted structural equivalents and unsupported or over-limit adversarial fixtures.

##### Steps

Run variant detection and passive bounded preview across qualified, permission-encrypted, changed-layout, active-content, scan-only, password, malformed, and over-limit cases.

##### Expected

Every acceptance criterion of FR-INGEST-VARIANT-QUALIFICATION holds; only the two evidenced PDF variants qualify and all unsafe or unsupported cases fail before mutation.
