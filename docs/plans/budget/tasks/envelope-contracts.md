# Publish the envelope partition and ancestry provenance contract fields

> Generated from the lex graph. Do not edit this file as source of truth; update graph entities and re-export.

- **Ref:** `TASK-BUDGET-ENVELOPE-CONTRACTS`
- **Plan:** `PLAN-BUDGET-V1`
- **Sub-Plan:** `SP-BUDGET-02-POSITION-PROJECTION`
- **State:** `ready`
- **Priority:** `1`
- **Sort Order:** `20`
- **Dialect:** `default`

## Objective

The Budget Position and INSIGHTS member contracts carry the direct/descendant partition and frozen Spend Category ancestry, and the calculation schema version advances to `budget-position-v2`.

## References

| Ref | Type | Relationship | Required |
|---|---|---|---|
| DD-BUDGET-CATEGORY-ENVELOPE-RESOLUTION: Nearest-ancestor Category Budget Entry resolution over frozen Spend Category ancestry | `design_decision` | `governed-by` | `true` |
| DM-BUDGET-POSITION-PROJECTION: BudgetPositionProjection | `data_model` | `touches` | `true` |
| FR-BUDGET-INSIGHTS-PROJECTION: Expose a read-only INSIGHTS projection | `requirement` | `touches` | `true` |
| FR-BUDGET-POSITION-QUERY: Calculate exact Budget Positions | `requirement` | `implements` | `true` |

## Dependencies

No task dependencies recorded.

## Recipe

### Acceptance Checks

- Given the CategoryPosition record, when its members are inspected, then DirectActualMinorUnits and DescendantActualMinorUnits are present as required long values and AbsorbedCategoryIds is present as a nullable string list.
- Given the BudgetActualMember record, when its members are inspected, then AncestryIds is present as a required string list and EffectiveCategoryId is present as a nullable string.
- Given BudgetPositionCalculator.CalculationSchemaVersion, when its value is read, then it equals the literal `budget-position-v2`.
- Given the published budget.position.get and budget.insights.evidence.get result schemas, when tally schema show is invoked for each, then the new fields appear and the reported contractVersion remains 1.0.

### Failure Criteria

- Do NOT advance the public BUDGET contract version past 1.0 — the added result fields are additive and the calculation schema version is the compatibility signal, per DD-BUDGET-CATEGORY-ENVELOPE-RESOLUTION.
- Do NOT add a request-side flag or mode selector to choose exact-identity behavior — rejected alternative C in DD-BUDGET-CATEGORY-ENVELOPE-RESOLUTION, because a flat taxonomy is already unchanged.
- Do NOT compute or populate any resolution value in this task — the contract surface lands first and TASK-BUDGET-ENVELOPE-RESOLUTION owns the calculation.

### Expected Outputs

- CategoryPosition carrying the direct/descendant partition and absorbed identifier list
- BudgetActualMember carrying frozen ancestry and the effective Spend Category identifier
- CalculationSchemaVersion advanced to `budget-position-v2`

### Constraints

- Source-generated JSON only; register any new type shape in the existing BudgetJsonContext rather than adding reflection-based serialization.
- Int64 minor units for every new amount field; no decimal or floating-point money.

### Notes

- AncestryIds mirrors LEDGER ActualsPageItem.FrozenAncestryIds exactly: root-first with the assigned Spend Category as the final element, and empty when the member has no assigned Spend Category.

### File Contracts

| Path | Action | Role | Required | Notes |
|---|---|---|---|---|
| `src/Tally/Contracts/Budget/Position/BudgetPositionContracts.cs` | `modify` | contract | `true` |  |
| `src/Tally/Contracts/Budget/Insights/BudgetInsightEvidence.cs` | `modify` | contract | `true` |  |
| `src/Tally/Domain/Budget/Position/BudgetPositionCalculator.cs` | `modify` | implementation | `true` | CalculationSchemaVersion constant only in this task |
| `src/Tally/Contracts/Budget/BudgetJsonContext.cs` | `modify` | serialization | `false` |  |
| `tests/Tally.Tests/Budget/Contract/BudgetContractShapeTests.cs` | `create` | test | `true` |  |

### Interface Contracts

| Name | Direction | Contract | Notes |
|---|---|---|---|
| CategoryPosition.DirectActualMinorUnits | `produces` | DM-BUDGET-POSITION-PROJECTION |  |
| CategoryPosition.DescendantActualMinorUnits | `produces` | DM-BUDGET-POSITION-PROJECTION |  |
| CategoryPosition.AbsorbedCategoryIds | `produces` | DM-BUDGET-POSITION-PROJECTION |  |
| BudgetActualMember.AncestryIds | `produces` | DM-BUDGET-POSITION-PROJECTION |  |
| BudgetActualMember.EffectiveCategoryId | `produces` | DM-BUDGET-POSITION-PROJECTION |  |
| BudgetPositionCalculator.CalculationSchemaVersion | `produces` | DM-BUDGET-POSITION-PROJECTION |  |

### Verification

| Phase | Command | Expected | Required | Timeout |
|---|---|---|---|---:|
| `after` | `dotnet build Tally.slnx --nologo` | Build succeeded with 0 errors and 0 warnings | `true` | 300 |
| `after` | `dotnet test tests/Tally.Tests/Tally.Tests.csproj --filter FullyQualifiedName~BudgetContractShapeTests` | All discovered BudgetContractShapeTests pass, exit code 0, at least 4 tests run | `true` | 300 |

### Review Gates

| Gate | Description | Required |
|---|---|---|
| `self-review` | Confirm contractVersion is still 1.0 and only the calculation schema version advanced | `true` |

## Bead References

No bead references recorded.

## Graph Trace

Generated from task provenance, task dependency, task reference, and bead-ref graph rows.

- `governed-by` -> DD-BUDGET-CATEGORY-ENVELOPE-RESOLUTION: Nearest-ancestor Category Budget Entry resolution over frozen Spend Category ancestry
- `implements` -> FR-BUDGET-POSITION-QUERY: Calculate exact Budget Positions
- `touches` -> DM-BUDGET-POSITION-PROJECTION: BudgetPositionProjection
- `touches` -> FR-BUDGET-INSIGHTS-PROJECTION: Expose a read-only INSIGHTS projection

## Navigation

- Sub-plan: [SP-BUDGET-02-POSITION-PROJECTION](../sub-plans/02-position-projection.md)
- Plan overview: [overview](../overview.md)
