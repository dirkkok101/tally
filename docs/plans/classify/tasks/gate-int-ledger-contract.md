# Prove the Ledger classification prerequisite

> Generated from the lex graph. Do not edit this file as source of truth; update graph entities and re-export.

- **Ref:** `TASK-CLASSIFY-GATE-INT-LEDGER-CONTRACT`
- **Plan:** `PLAN-CLASSIFY-V1`
- **Sub-Plan:** `SP-CLASSIFY-00-FOUNDATION`
- **State:** `planned`
- **Priority:** `0`
- **Sort Order:** `2`
- **Dialect:** `default`

## Summary

Validation-only integration gate with no implements refs; the producer task delivers behavior and this gate proves the complete released seam.

## Objective

Published Ledger descriptors, projection behavior, mutation preconditions, and private-boundary guards satisfy every CLASSIFY consumer before client work begins.

## References

| Ref | Type | Relationship | Required |
|---|---|---|---|
| DD-CLASSIFY-LEDGER-PUBLIC-PROJECTION: Use purpose-scoped classification projections on the public Ledger actuals contract | `design_decision` | `governed-by` | `true` |
| DM-CLASSIFY-LEDGER-PROJECTION-CONTRACT: LedgerClassificationProjectionContracts | `data_model` | `touches` | `true` |
| EXT-CLASSIFY-LEDGER-PUBLIC-CONTRACT: LEDGER Public Contract | `external_dependency` | `references` | `true` |
| TASK-LEDGER-GATE-INT-PUBLIC-CONTRACT: Wire and prove the complete public CLI contract | `task` | `blocked-by` | `true` |
| TC-CLASSIFY-APPLY-EXECUTION-CONTRACT: Verify replay-safe classification apply | `test_case` | `verifies` | `true` |
| TC-CLASSIFY-ELIGIBLE-PROJECTION-CONTRACT: Verify the eligible classification projection | `test_case` | `verifies` | `true` |

## Dependencies

| Depends On | Type | Reason |
|---|---|---|
| [TASK-CLASSIFY-LEDGER-CLASSIFICATION-CONTRACT](../tasks/ledger-classification-contract.md) | `compile` | The gate executes the projection and mutation contracts produced by this task. |

## Recipe

### Acceptance Checks

- The released registry exposes classification_v1 evaluation and apply_preflight schemas plus revision-aware assign and correct schemas.
- A multi-page evaluation projection preserves membership and ordinals; incompatible versions and expired cursors create no partial evaluation input.
- Every apply-preflight ID is represented and every stale allocation or lifecycle revision is rejected before Ledger mutation.
- Architecture scans find zero CLASSIFY references to LedgerDb, Ledger storage paths, private handlers, or private domain types.

### Failure Criteria

- Do NOT mark this gate green from schema presence alone; execute public operations through the released registry.
- Do NOT translate or weaken Ledger errors in a test adapter.
- Do NOT continue to client implementation if the blocked-by Ledger public-contract gate is absent or failing.

### Expected Outputs

- VerifiedLedgerClassificationContract integration evidence

### Constraints

- This validation-only gate is intentionally loose and creates no product behavior.

### Notes

- Failure routes contract work back to LEDGER; it does not authorize a CLASSIFY workaround.

### File Contracts

| Path | Action | Role | Required | Notes |
|---|---|---|---|---|
| `src/Tally/Cli/OperationRegistry.cs` | `read` | released descriptor registry | `true` |  |
| `src/Tally/Contracts/Ledger/Actuals/ActualsContracts.cs` | `read` | projection schema | `true` |  |
| `src/Tally/Contracts/Ledger/Transactions/CategoryAllocationContracts.cs` | `read` | mutation schema | `true` |  |
| `src/Tally/Features/Ledger/Actuals/ActualsQueryHandler.cs` | `read` | projection implementation | `true` |  |
| `src/Tally/Features/Ledger/Transactions/CategoryAllocationHandlers.cs` | `read` | precondition implementation | `true` |  |
| `tests/Tally.Tests/Classify/LedgerContract/LedgerClassifyPrerequisiteTests.cs` | `test` | integration gate | `true` |  |

### Interface Contracts

| Name | Direction | Contract | Notes |
|---|---|---|---|
| ClassificationProjectionRequest | `consumes` | DM-CLASSIFY-LEDGER-PROJECTION-CONTRACT |  |
| LedgerCategoryMutationPreconditions | `consumes` | DM-CLASSIFY-LEDGER-PROJECTION-CONTRACT |  |
| VerifiedLedgerClassificationContract | `produces` | DM-CLASSIFY-LEDGER-PROJECTION-CONTRACT |  |

### Verification

| Phase | Command | Expected | Required | Timeout |
|---|---|---|---|---:|
| `after` | `dotnet test tests/Tally.Tests/Tally.Tests.csproj --filter FullyQualifiedName~LedgerClassifyPrerequisiteTests` | exit 0; at least 18 descriptor, paging, eligibility, preflight, stale-write, version, and private-boundary cases are discovered and 0 fail | `true` | 480 |

### Review Gates

| Gate | Description | Required |
|---|---|---|
| `branch-review` | Review the public seam against the blocked-by Ledger plan outputs before downstream tasks proceed. | `true` |

## Bead References

No bead references recorded.

## Graph Trace

Generated from task provenance, task dependency, task reference, and bead-ref graph rows.

- `blocked-by` -> TASK-LEDGER-GATE-INT-PUBLIC-CONTRACT: Wire and prove the complete public CLI contract
- `depends-on:compile` -> [TASK-CLASSIFY-LEDGER-CLASSIFICATION-CONTRACT](../tasks/ledger-classification-contract.md): The gate executes the projection and mutation contracts produced by this task.
- `governed-by` -> DD-CLASSIFY-LEDGER-PUBLIC-PROJECTION: Use purpose-scoped classification projections on the public Ledger actuals contract
- `references` -> EXT-CLASSIFY-LEDGER-PUBLIC-CONTRACT: LEDGER Public Contract
- `touches` -> DM-CLASSIFY-LEDGER-PROJECTION-CONTRACT: LedgerClassificationProjectionContracts
- `verifies` -> TC-CLASSIFY-APPLY-EXECUTION-CONTRACT: Verify replay-safe classification apply
- `verifies` -> TC-CLASSIFY-ELIGIBLE-PROJECTION-CONTRACT: Verify the eligible classification projection

## Navigation

- Sub-plan: [SP-CLASSIFY-00-FOUNDATION](../sub-plans/00-foundation.md)
- Plan overview: [overview](../overview.md)
