# Prove the public LEDGER prerequisite

> Generated from the lex graph. Do not edit this file as source of truth; update graph entities and re-export.

- **Ref:** `TASK-INGEST-GATE-INT-LEDGER-CONTRACT`
- **Plan:** `PLAN-INGEST-V1`
- **Sub-Plan:** `SP-INGEST-00-FOUNDATION`
- **State:** `planned`
- **Priority:** `0`
- **Sort Order:** `10`
- **Dialect:** `default`

## Summary

Integration gate intentionally has no INGEST Implements link: it proves the upstream producer seam before any INGEST consumer is allowed to compile. Lex task dependencies are plan-local, so TASK-LEDGER-GATE-INT-PUBLIC-CONTRACT is recorded as required blocked-by context and must complete operationally first.

## Objective

Prove that the released LEDGER registry exposes the exact account-read and idempotent transaction record/get contracts INGEST requires without private storage or handler access.

## References

| Ref | Type | Relationship | Required |
|---|---|---|---|
| DD-INGEST-LEDGER-PUBLIC-INTEGRATION: Invoke LEDGER through the shared public operation executor | `design_decision` | `governed-by` | `true` |
| TASK-LEDGER-GATE-INT-PUBLIC-CONTRACT: Wire and prove the complete public CLI contract | `task` | `blocked-by` | `true` |
| TC-INGEST-LEDGER-PUBLIC-CONFORMANCE: Verify INGEST uses only the public LEDGER contract | `test_case` | `verifies` | `true` |

## Dependencies

No task dependencies recorded.

## Recipe

### Acceptance Checks

- Given completed TASK-LEDGER-GATE-INT-PUBLIC-CONTRACT, OperationRegistry exposes ledger.account.get, ledger.transaction.record, and ledger.transaction.get with concrete versioned source-generated schemas and documented stable errors/exits.
- ledger.account.get returns AccountDetail with accountId, accountClass, currencyCode, and active or archived status; ledger.transaction.record/get round-trip every immutable fact required by DM-INGEST-LEDGER-COMMIT-CONTRACT.
- Replaying ledger.transaction.record with the same idempotency key and byte-equivalent logical input returns the prior logical result; the same key with different normalized input returns LEDGER-IDEMPOTENCY-001 and creates no second transaction.
- The test compiles using only Contracts/Ledger, Application operation abstractions, and OperationRegistry; no LedgerDb, storage path, SQL, feature handler, or private domain reference is accessible.

### Failure Criteria

- Do NOT open LedgerDb or SQL, call LEDGER feature handlers, or reference private Ledger domain types — rejected per DD-INGEST-LEDGER-PUBLIC-INTEGRATION.
- Do NOT spawn tally as a child process for in-process composition — rejected per DD-INGEST-LEDGER-PUBLIC-INTEGRATION.
- Do NOT weaken or duplicate canonical LEDGER validation inside the gate.

### Expected Outputs

- LedgerIngestPrerequisite contract test
- Public account and transaction seam evidence

### Constraints

- Treat upstream Ledger operation descriptors and contract records as read-only public dependencies.

### Notes

- Removal impact: none; this task validates planned LEDGER behavior and changes no LEDGER implementation.

### File Contracts

| Path | Action | Role | Required | Notes |
|---|---|---|---|---|
| `src/Tally/Application/OperationAbstractions.cs` | `read` | shared public executor contract | `true` |  |
| `src/Tally/Cli/OperationRegistry.cs` | `read` | released operation registry | `true` |  |
| `src/Tally/Contracts/Ledger/Accounts/AccountContracts.cs` | `read` | public account contract | `true` |  |
| `src/Tally/Contracts/Ledger/Transactions/TransactionContracts.cs` | `read` | public transaction contract | `true` |  |
| `tests/Tally.Tests/Ingest/LedgerContract/LedgerIngestPrerequisiteTests.cs` | `test` | integration gate | `true` |  |

### Interface Contracts

| Name | Direction | Contract | Notes |
|---|---|---|---|
| VerifiedLedgerOperationRegistry | `produces` | DM-LEDGER-OPERATION-DESCRIPTOR | Validated external OperationRegistry from the blocked-by LEDGER gate |
| VerifiedLedgerAccountDetail | `produces` | DM-LEDGER-ACCOUNT-CATEGORY-CONTRACTS | Validated external AccountDetail contract |
| VerifiedLedgerTransactionDetail | `produces` | DM-LEDGER-TRANSACTION-CONTRACTS | Validated external TransactionDetail contract |
| LedgerIngestContractPrerequisite | `produces` | DM-INGEST-LEDGER-COMMIT-CONTRACT | Local proof that upstream public contracts satisfy INGEST |

### Verification

| Phase | Command | Expected | Required | Timeout |
|---|---|---|---|---:|
| `after` | `dotnet test tests/Tally.Tests/Tally.Tests.csproj --filter FullyQualifiedName~LedgerIngestPrerequisiteTests` | exit 0; at least 8 prerequisite cases are discovered, exactly the three required operation descriptors are exercised, and 0 tests fail | `true` | 300 |

### Review Gates

| Gate | Description | Required |
|---|---|---|
| `test-evidence` | Attach descriptor, replay, conflict, and private-reference guard results. | `true` |
| `branch-review` | Reviewer confirms every consumed type is a released public contract. | `true` |

## Bead References

No bead references recorded.

## Graph Trace

Generated from task provenance, task dependency, task reference, and bead-ref graph rows.

- `blocked-by` -> TASK-LEDGER-GATE-INT-PUBLIC-CONTRACT: Wire and prove the complete public CLI contract
- `governed-by` -> DD-INGEST-LEDGER-PUBLIC-INTEGRATION: Invoke LEDGER through the shared public operation executor
- `verifies` -> TC-INGEST-LEDGER-PUBLIC-CONFORMANCE: Verify INGEST uses only the public LEDGER contract

## Navigation

- Sub-plan: [SP-INGEST-00-FOUNDATION](../sub-plans/00-foundation.md)
- Plan overview: [overview](../overview.md)
