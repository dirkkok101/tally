# Implement module-wide retained-operation replay

> Generated from the lex graph. Do not edit this file as source of truth; update graph entities and re-export.

- **Ref:** `TASK-INSIGHTS-IDEMPOTENCY-EXECUTOR`
- **Plan:** `PLAN-INSIGHTS-V1`
- **Sub-Plan:** `SP-INSIGHTS-02-RETAINED-HISTORY`
- **State:** `planned`
- **Priority:** `0`
- **Sort Order:** `1`
- **Dialect:** `default`

## Summary

Create the transactional Attempting and Succeeded replay coordinator consumed by Retain, Restate, Delete, Backup, and Restore.

## Objective

InsightsIdempotencyExecutor returns prior logical outcomes for exact retries, rejects changed input, and never consumes failed request identities or leaks deleted content.

## References

| Ref | Type | Relationship | Required |
|---|---|---|---|
| DD-INSIGHTS-RETENTION-RESTATEMENT-LIFECYCLE: Append-only snapshots with replay-safe restatement and leaf deletion | `design_decision` | `governed-by` | `true` |
| DD-INSIGHTS-VERIFIED-RECOVERY-ACTIVATION: Verified backup candidates with atomic generation activation | `design_decision` | `governed-by` | `true` |
| DM-INSIGHTS-IDEMPOTENCY: InsightsIdempotencyRecord | `data_model` | `touches` | `true` |
| NFR-INSIGHTS-ATOMIC-DURABLE-REPORT-STATE: Make retained report changes atomic and durable | `nfr` | `satisfies` | `true` |
| TC-INSIGHTS-REPORT-RETENTION-IDEMPOTENCY: Verify retained generation and idempotent replay | `test_case` | `verifies` | `true` |

## Dependencies

| Depends On | Type | Reason |
|---|---|---|
| [TASK-INSIGHTS-STATE-FOUNDATION](../tasks/state-foundation.md) | `compile` | The replay executor uses the idempotency table and caller-owned SQLite transactions. |

## Recipe

### Acceptance Checks

- The unique replay identity is operationId plus contractVersion plus keyDigest; the canonical request fingerprint includes every logical input, actor, reason, target or predecessor, selected policies, expected-current fingerprint, and safe normalized path identity where applicable.
- A Succeeded record with the same key and fingerprint returns the exact prior logical outcome before TimeProvider, producer, or write access; the same key with a changed fingerprint returns IdempotencyConflict and changes nothing.
- New write attempts are reserved transactionally; success stores one immutable outcome reference; validation, cancellation, transient, generation, and persistence failure leave no successful outcome and remove any completed-failure reservation.
- If a retained snapshot outcome was later deleted, replay resolves to metadata-only Deleted evidence and never recreates or discloses the removed payload.
- Backup-specific Attempting reconciliation can retain deterministic artifact identity across a crash while the copied artifact excludes the current non-terminal reservation.

### Failure Criteria

- Do NOT store full requests, reports, private paths, or caller idempotency keys; persist digests, fingerprints, safe identities, and outcome refs only.
- Do NOT treat expected failures as exceptions, consume a failed request identity, create a second logical outcome, or replay deleted content.
- Do NOT implement a generic event-sourcing system — rejected per DD-INSIGHTS-RETENTION-RESTATEMENT-LIFECYCLE.

### Expected Outputs

- InsightsIdempotencyExecutor
- Replay conflict and crash-state tests

### Constraints

- The executor participates in the caller-owned SQLite transaction and has one production implementation.

### Notes

- Recovery handlers use the same identity model but own their external artifact saga.

### File Contracts

| Path | Action | Role | Required | Notes |
|---|---|---|---|---|
| `src/Tally/Infrastructure/Insights/Storage/Idempotency/InsightsIdempotencyExecutor.cs` | `create` | transactional replay coordinator | `true` |  |
| `tests/Tally.Tests/Insights/Storage/InsightsIdempotencyExecutorTests.cs` | `test` | same, conflict, failure, deleted, and recovery reservation tests | `true` |  |

### Interface Contracts

| Name | Direction | Contract | Notes |
|---|---|---|---|
| InsightsStateStore | `consumes` | DM-INSIGHTS-STATE-STORE | transaction and replay rows |
| InsightsIdempotencyExecutor.ExecuteAsync | `produces` | DM-INSIGHTS-IDEMPOTENCY | new, replay, conflict, or failed reservation result |

### Verification

| Phase | Command | Expected | Required | Timeout |
|---|---|---|---|---:|
| `after` | `dotnet test tests/Tally.Tests/Tally.Tests.csproj --filter FullyQualifiedName~InsightsIdempotencyExecutorTests` | exit 0; at least 18 exact-replay, changed-input conflict, validation failure, cancellation, rollback, crash reservation, deleted-outcome, path-digest, payload-exclusion, and concurrent-key cases are discovered and 0 fail | `true` | 420 |

### Review Gates

| Gate | Description | Required |
|---|---|---|
| `test-evidence` | Review transaction boundaries and prove every failure leaves no successful or duplicate outcome. | `true` |

## Bead References

No bead references recorded.

## Graph Trace

Generated from task provenance, task dependency, task reference, and bead-ref graph rows.

- `depends-on:compile` -> [TASK-INSIGHTS-STATE-FOUNDATION](../tasks/state-foundation.md): The replay executor uses the idempotency table and caller-owned SQLite transactions.
- `governed-by` -> DD-INSIGHTS-RETENTION-RESTATEMENT-LIFECYCLE: Append-only snapshots with replay-safe restatement and leaf deletion
- `governed-by` -> DD-INSIGHTS-VERIFIED-RECOVERY-ACTIVATION: Verified backup candidates with atomic generation activation
- `satisfies` -> NFR-INSIGHTS-ATOMIC-DURABLE-REPORT-STATE: Make retained report changes atomic and durable
- `touches` -> DM-INSIGHTS-IDEMPOTENCY: InsightsIdempotencyRecord
- `verifies` -> TC-INSIGHTS-REPORT-RETENTION-IDEMPOTENCY: Verify retained generation and idempotent replay

## Navigation

- Sub-plan: [SP-INSIGHTS-02-RETAINED-HISTORY](../sub-plans/02-retained-history.md)
- Plan overview: [overview](../overview.md)
