# Implement the V001 catalogue schema fragment

> Generated from the lex graph. Do not edit this file as source of truth; update graph entities and re-export.

- **Ref:** `TASK-LEDGER-CORE-SCHEMA-CATALOGUE`
- **Plan:** `PLAN-LEDGER-V1`
- **Sub-Plan:** `SP-LEDGER-01-CORE-RUNTIME`
- **State:** `planned`
- **Priority:** `0`
- **Sort Order:** `21`
- **Dialect:** `default`

## Summary

Schema foundation intentionally has no Implements link: translate the approved account, category, and catalogue-lifecycle models into one independently verified V001 fragment.

## Objective

Create exact account, spend-category, and attributable catalogue-lifecycle tables, constraints, indexes, and active-name uniqueness without implementing catalogue behavior.

## References

| Ref | Type | Relationship | Required |
|---|---|---|---|
| DD-LEDGER-EMBEDDED-STORAGE: Raw SQLite with host-managed at-rest protection | `design_decision` | `governed-by` | `true` |
| DD-LEDGER-IMMUTABLE-HISTORY: Immutable financial facts with append-only lifecycle history | `design_decision` | `governed-by` | `true` |
| DM-LEDGER-ACCOUNT: Account | `data_model` | `touches` | `true` |
| DM-LEDGER-CATALOGUE-LIFECYCLE: CatalogueLifecycleEvents | `data_model` | `touches` | `true` |
| DM-LEDGER-SPEND-CATEGORY: SpendCategory | `data_model` | `touches` | `true` |

## Dependencies

| Depends On | Type | Reason |
|---|---|---|
| [TASK-LEDGER-CORE-STORAGE](../tasks/core-storage.md) | `compile` | Consumes LedgerDb and LedgerSchemaFragmentRegistry. |

## Recipe

### Acceptance Checks

- V001CatalogueSchema creates every field, enum check, lifecycle foreign key, RESTRICT rule, status timestamp rule, and active uniqueness constraint in the three linked models.
- Account active masked identity is unique within institution; category active normalized name is unique; archive history remains attributable and append-only.
- Applying the fragment twice in one migration plan is rejected before SQL; an injected statement failure rolls back every catalogue table and index.

### Failure Criteria

- Do NOT create transaction, allocation, relationship, snapshot, recovery, or idempotency tables in this fragment.
- Do NOT add hard delete, account reactivation, category hierarchy, full account numbers, EF Core, or mocked SQLite tests.

### Expected Outputs

- V001CatalogueSchema
- LedgerCatalogueSchemaTests real-SQLite constraint tests

### Constraints

- Implement an explicit V001 fragment only; TASK-LEDGER-GATE-INT-CORE owns complete-schema composition.

### Notes

None recorded.

### File Contracts

| Path | Action | Role | Required | Notes |
|---|---|---|---|---|
| `src/Tally/Infrastructure/Storage/Migrations/V001/V001CatalogueSchema.cs` | `create` | schema fragment | `true` |  |
| `tests/Tally.Tests/Infrastructure/Storage/LedgerCatalogueSchemaTests.cs` | `test` | real SQLite schema verification | `true` |  |

### Interface Contracts

| Name | Direction | Contract | Notes |
|---|---|---|---|
| V001CatalogueSchema | `produces` | DM-LEDGER-ACCOUNT |  |
| LedgerSchemaFragmentRegistry | `consumes` | DD-LEDGER-EMBEDDED-STORAGE |  |
| LedgerDb | `consumes` | DM-LEDGER-STORE-GENERATION |  |

### Verification

| Phase | Command | Expected | Required | Timeout |
|---|---|---|---|---:|
| `after` | `dotnet test tests/Tally.Tests/Tally.Tests.csproj --filter FullyQualifiedName~Tally.Tests.Infrastructure.Storage.LedgerCatalogueSchemaTests --no-restore` | exit 0; at least 10 real-SQLite table/index/foreign-key/check/rollback cases run and 0 fail | `true` | 240 |

### Review Gates

| Gate | Description | Required |
|---|---|---|
| `test-evidence` | Show exact table, index, foreign-key, check, uniqueness, and rollback evidence for every linked model. | `true` |
| `self-review` | The fragment contains every linked model field and no tables owned by another fragment. | `true` |

## Bead References

No bead references recorded.

## Graph Trace

Generated from task provenance, task dependency, task reference, and bead-ref graph rows.

- `depends-on:compile` -> [TASK-LEDGER-CORE-STORAGE](../tasks/core-storage.md): Consumes LedgerDb and LedgerSchemaFragmentRegistry.
- `governed-by` -> DD-LEDGER-EMBEDDED-STORAGE: Raw SQLite with host-managed at-rest protection
- `governed-by` -> DD-LEDGER-IMMUTABLE-HISTORY: Immutable financial facts with append-only lifecycle history
- `touches` -> DM-LEDGER-ACCOUNT: Account
- `touches` -> DM-LEDGER-CATALOGUE-LIFECYCLE: CatalogueLifecycleEvents
- `touches` -> DM-LEDGER-SPEND-CATEGORY: SpendCategory

## Navigation

- Sub-plan: [SP-LEDGER-01-CORE-RUNTIME](../sub-plans/01-core-runtime.md)
- Plan overview: [overview](../overview.md)
