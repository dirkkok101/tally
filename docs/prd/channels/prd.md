# Interaction Channels

> Generated from the lex graph. Do not edit this file as source of truth; update graph entities and re-export.

| Field | Value |
|-------|-------|
| **Module Code** | CHANNELS |
| **Scope** | standard |
| **Description** | Owns optional human interaction over stable Tally contracts; no channel is selected yet. A WhatsApp or Hermes-like path may remain an external agent adapter over the CLI. If a web interface is selected, the architecture must use a PostgreSQL database through Entity Framework Core, a backend on the latest stable .NET available to the project, and a frontend on the latest stable Angular using the NxGN UI npm package. That web choice implies a service boundary and must be resolved before technical design rather than treated as a cosmetic frontend addition. Awaiting its own brainstorm. |
| **Why Now** | The boundary must be explicit now so CLI design remains channel-neutral while human interaction is deferred. |

## Problem Statement

Dirk needs convenient access to Tally commands and insights, but choosing a web application or WhatsApp-style interface before the CLI contract is stable would couple the finance core to an uncertain delivery channel.

## Constraints

| Ref | Constraint | Type |
|-----|------------|------|
| C1 | Conditional NxGN web stack: If Tally selects a web interface, persistence uses PostgreSQL through Entity Framework Core; the backend targets the project's latest stable .NET; the frontend targets the project's latest stable Angular and uses the NxGN UI npm package. Exact installed package versions and package identity are verified from project files when the web module is designed; downgrades are not an option. | technical |
